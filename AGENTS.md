# UXUCode 项目协作规则

语言：简体中文。

本文件适用于仓库根目录及全部子目录。`Claude/`、`Codex/`、`OpenClaw/` 内更具体的宿主规则可以补充宿主差异，但不得削弱本文件的事实源、无兼容和安全边界。

## 1. 规范优先级

1. 用户当前明确要求；
2. 已批准的 `work-products/SPEC.md`；
3. `work-products/plan.md` 与 `work-products/todo.md`；
4. 当前源码、测试和工作区证据；
5. 历史文件、已安装插件缓存或旧版约定。

当前项目状态用于确定实现位置、风险和验证方法，不得反向改变已批准的新产品规范。发现旧文件或旧缓存时，将其视为待整理证据，不得据此新增兼容层、别名、双写、回退读取或项目专属产品分支。

如果实现证据与已批准规格冲突，停止并说明冲突；先修订规格并获得批准，不得静默让实现迁就旧状态。

## 2. 唯一工作流事实源

UXUCode 过程产物只使用：

```text
work-products/
├── SPEC.md
├── plan.md
├── todo.md
├── debug/
├── reviews/
├── ship/
└── tests/
```

- 不创建或读取根目录 `SPEC.md`、`tasks/plan.md`、`tasks/todo.md` 等旧路径。
- 不为旧路径提供兼容、别名、双写、回退读取或 `.gitignore` 隐藏规则。
- 正式规格、计划、任务清单和持久合同测试应允许正常 Git 跟踪，不得依赖 `git add -f`。
- debug、review、ship 及其他未声明的本地过程文件默认忽略。
- 产品源码和最终交付物继续遵循项目原生位置。

## 3. 公开命令与阶段边界

- Codex 只使用 `@<command>`；不得推断 Claude slash command、别名或带标点变体。
- `@spec` 只定义规格，不实现业务代码。
- `@plan` 只根据已批准规格生成依赖有序、可验证的计划，不实现业务代码。
- `@build` 只执行下一个已批准任务；`@build auto` 才可连续执行稳定计划。
- `@review` 只审查当前实现，不把审查自动扩大为提交、发布或部署。
- 新增公开命令必须同步 Claude/Codex Skill、路由器、help、插件校验、命令一致性校验和三语言文档。

## 4. `clean` 新规范

`clean` 是工作区过程产物整理 Skill，不是通用删除命令：

- Codex：`@clean` 预览，`@clean apply` 执行；
- Claude Code：`/uxu-code:clean` 预览，`/uxu-code:clean apply` 执行；
- 处理能够确认属于 UXUCode 且错放在 `work-products/` 外的 SPEC、task 及相关过程文件；全仓内可按受支持命名约定明确识别的内部测试统一迁入 `work-products/tests/`；
- 受支持测试命名覆盖跨语言的分隔式 `test`／`spec`、`test_*`／`*_test` 及 CamelCase `*Test(s).*`；任意层级的版本控制和第三方依赖目录不扫描；
- 项目源码、交付文件和归属不明确的非测试文件不移动；
- 移动前构建完整映射，并同步修正有效相对路径、迁移文件内的仓库内绝对路径及对迁移源的精确引用；
- 既有或计划内目标冲突、目标祖先链接／逃逸、路径歧义或无法安全重写时返回 `BLOCKED`，不得覆盖或部分执行；无法证明具有路径语义的裸字符串不自动改写；
- 默认预览必须零写入；成功执行后再次预览应为 `NO_CHANGES`。

不得因为当前仓库存在旧文件而给 `clean` 增加旧版兼容或项目专属行为。当前仓库只能作为通用合同的一个验证样本。

## 5. 实施与修改边界

- 开始编辑前检查相关 diff，保留所有用户修改和无关改动。
- 每一处改动必须可追溯到已批准规格和当前任务。
- 优先复用现有实现和 Node.js 标准库；不为单次用途增加抽象或第三方依赖。
- 不擅自整理、格式化、删除或重构邻近代码。
- 文件移动或内容重写前必须完成冲突、引用和回滚预检。
- 未经明确授权，不暂存、提交、推送、发布、部署、重装插件或修改已安装插件缓存。

## 6. CodeGraph 与验证

- 仓库根目录存在 `.codegraph/` 时，理解或定位代码必须先使用 CodeGraph；配置和纯文档可直接读取。
- 行为修改先建立失败证据，再完成最小修复，并运行目标测试与相关回归。
- 统一静态门禁为：

```powershell
node scripts/validate-all.js
git -c safe.directory=C:/Code/UXUCode diff --check
```

- 静态校验、本地测试、真实宿主加载和生产行为是不同证据层级，不得互相替代。
- 安装缓存未重新加载时，必须明确说明仓库源码已更新但当前会话可能仍运行旧版本。
